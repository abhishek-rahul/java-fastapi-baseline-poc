# java-fastapi-baseline-poc

The Java client supports `HTTP` and `MANAGED_RUNTIME` execution modes. See the
[Managed Python Runtime operations runbook](docs/codex/OPERATIONS_RUNBOOK.md) for launch,
health, recovery, shutdown, and rollback guidance.
